# 한국육상매거진 카드뉴스 운영 에이전트 지침

이 프로젝트는 한국육상매거진 인스타그램 업로드용 카드뉴스 제작 도구다. 사용자는 텔레그램이나 Codex에서 한국어로 지시한다.

## 프로젝트 기본 정보

- 작업 폴더: `/Users/ahnhyeonji/Documents/Codex/2026-05-11/files-mentioned-by-the-user-img`
- 배포 앱: `https://kaaf-result-card-maker.onrender.com`
- GitHub 업로드: `https://github.com/hyeonjiiii1020/new/upload/main`
- 로컬 실행: `PORT=5174 node "server 3.js"` 또는 `npm start`
- 주요 파일: `server.js`, `server 3.js`, `app.js`, `public/app.js`, `styles.css`, `public/styles.css`, `index.html`, `public/index.html`

## 작업 원칙

- 사용자가 기능 수정을 요청하면 구현 후 반드시 검토와 검증까지 진행한다.
- `app.js`와 `public/app.js`, `styles.css`와 `public/styles.css`, `index.html`과 `public/index.html`, `server.js`와 `server 3.js`는 서로 동기화되어야 한다.
- 업로드가 필요한 변경은 ZIP 파일을 만들어 사용자가 GitHub에 올릴 수 있게 한다.
- 검증용 임시 파일을 만들었다면 완료 후 삭제한다.
- 결과물은 인스타그램 피드에서 잘리지 않도록 기본 1080x1350 크기를 유지한다.

## 결과 카드 규칙

- 개인 트랙 종목: `순위 - 성명 - 소속 - 기록`
- 계주 종목: `순위 - 소속 - 성명 - 기록`
- 계주 성명은 4명 안팎이 들어가므로 글씨가 잘리지 않게 자동 축소한다.
- 10종/8종/7종 등 혼성 종목 종합 결과: `순위 - 성명 - 소속 - 총점`
- 결승 결과는 heat 기준이 아니라 전체 기록 기준 통합 랭킹으로 처리한다.
- DNS, DNF, 기권, 실격은 결과 카드에 넣지 않는다.
- 오른쪽 하단에는 작게 `한국육상매거진`을 출처처럼 표시한다.

## 시간표 카드 규칙

- 결과 카드 기능은 유지하고 시간표 카드만 별도로 수정한다.
- JPG/PNG/WEBP 사진 여러 장 업로드를 지원한다.
- 사진 텍스트 OCR에 의존하지 않는다. 사용자가 올린 시간표 사진은 앱 톤에 맞춰 재구성하거나 사진 카드 방식으로 넣는다.
- 트랙 1장, 필드 1장처럼 여러 장을 한꺼번에 만들 수 있어야 한다.

## 검증 체크리스트

- `/api/tournaments`가 현재 대회 코드를 반환하는지 확인한다.
- `/api/events`에서 필요한 종목이 드롭다운에 나타나는지 확인한다.
- `/api/result`에서 대표 종목 결과가 정상 rows를 반환하는지 확인한다.
- 계주는 4명 이름이 모두 합쳐지고 헤더가 `순위|소속|성명|기록`인지 확인한다.
- 브라우저 또는 검증 페이지로 PNG 캔버스가 1080x1350인지 확인한다.
- ZIP 구성에 루트 파일과 `public/` 파일이 모두 들어갔는지 확인한다.

## 콘텐츠 운영 방향

최종 목표는 단순 카드 제작이 아니라 한국육상매거진 운영 에이전트다.

- 대회 결과를 불러와 카드뉴스를 만든다.
- 흥미로운 기록, 접전, 다관왕 가능성, 유망주, 대회신기록/개인최고 가능성을 콘텐츠 후보로 제안한다.
- 인스타 게시물, 릴스, 스토리용 캡션과 해시태그를 제안한다.
- 반응 데이터를 받으면 추가 게시물 기획까지 제안한다.

## 텔레그램 지시 처리

텔레그램에서 지시가 오면 이 프로젝트 폴더 기준으로 작업한다. 애매한 요청은 최소한의 질문만 하고, 가능한 경우 직접 수정, 검증, 업로드 ZIP 생성까지 끝낸다.
