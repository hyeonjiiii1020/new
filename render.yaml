services:
  - type: web
    name: kaaf-result-card-maker
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node "server 3.js"
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
